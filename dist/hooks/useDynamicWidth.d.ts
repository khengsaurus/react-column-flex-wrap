import { IUseDynamicWidthProps } from "../types";
declare const useDynamicWidth: ({ containerRef, constantHeight, constantWidth, dependencies, }: IUseDynamicWidthProps) => void;
export default useDynamicWidth;
